import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  title: {
    color: '#ffffff',
    fontSize: 18,
    fontWeight: 'bold'
  },
  buttonGradient: {
    width: '100%',
    height: 50,
    borderRadius: 12,
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 10
  }
})